---
phase: 04-web-ui-integration
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/audioshuttle/web_routes/input_tab.py
  - src/audioshuttle/web_routes/output_tab.py
  - src/audioshuttle/templates/input.html
  - src/audioshuttle/templates/output.html
  - src/audioshuttle/web.py
autonomous: true

must_haves:
  truths:
    - "User can view and edit the E2B system prompt in a textarea"
    - "Saving the system prompt persists it (writes to config file)"
    - "User can see the DAW type (Reaper/Ardour) and change it"
    - "User can trigger a DAW rescan from the Output tab"
    - "OSC address mappings are visible in a table on the Output tab"
  artifacts:
    - path: "src/audioshuttle/web_routes/input_tab.py"
      provides: "GET/POST /input — system prompt editor + AI config"
      exports: ["router"]
    - path: "src/audioshuttle/web_routes/output_tab.py"
      provides: "GET/POST /output — DAW preset + mappings + rescan"
      exports: ["router"]
    - path: "src/audioshuttle/templates/input.html"
      provides: "System prompt textarea + AI connection display"
      contains: "textarea"
    - path: "src/audioshuttle/templates/output.html"
      provides: "DAW preset dropdown + OSC mappings table"
      contains: "osc-mapping"
  key_links:
    - from: "src/audioshuttle/web_routes/input_tab.py"
      to: "src/audioshuttle/translator.py"
      via: "SYSTEM_PROMPT read/write"
      pattern: "translator.*SYSTEM_PROMPT|system_prompt"
    - from: "src/audioshuttle/web_routes/output_tab.py"
      to: "src/audioshuttle/daw_detect.py"
      via: "detect_daw call on rescan"
      pattern: "detect_daw"
    - from: "src/audioshuttle/web.py"
      to: "src/audioshuttle/web_routes/input_tab.py"
      via: "include_router"
      pattern: "include_router.*input_tab"
---

<objective>
Create the Input (AI Connect) and Output (DAW Connect) tabs for the web configurator.

Purpose: The Input tab exposes the E2B system prompt for customization — this is the critical demo feature showing how the AI skill is editable. The Output tab shows DAW configuration, preset selection, and OSC mappings.

Output: Two new tabs accessible via nav links. Input tab has a system prompt textarea with save. Output tab has DAW preset dropdown, rescan button, and OSC mappings table.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-web-ui-integration/04-CONTEXT.md
@.planning/phases/04-web-ui-integration/04-RESEARCH.md
@.planning/phases/04-web-ui-integration/04-01-SUMMARY.md
@src/audioshuttle/translator.py
@src/audioshuttle/osc_bridge.py
@src/audioshuttle/daw_detect.py
@src/audioshuttle/config.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Input tab — system prompt editor + AI config</name>
  <files>src/audioshuttle/web_routes/input_tab.py, src/audioshuttle/templates/input.html</files>
  <action>
Create the Input (AI Connect) tab:

**web_routes/input_tab.py:**
- `router = APIRouter(prefix="/input")` — NO, use `router = APIRouter()` with routes at `/input` to match nav links
- Actually use prefix="/input" and templates will use "input.html"

Routes:
- `GET /input` — renders input.html with:
  - `system_prompt: str` — current SYSTEM_PROMPT from translator module (read `IntentTranslator.SYSTEM_PROMPT` or a module-level variable in translator.py that can be modified at runtime)
  - `ai_clients: list[dict]` — preset AI clients: `[{"name": "Gemini CLI", "description": "Google Gemini via CLI"}, {"name": "Claude Code", "description": "Anthropic Claude via CLI"}]` — hardcoded list for now
  - `chat_api_url: str` — from settings
  - `chat_model_name: str` — from settings
  - `saved: bool` — False by default, True after POST save (for showing save confirmation)

- `POST /input/system-prompt` — saves the system prompt:
  - Accepts `system_prompt: str = Form(...)` — the textarea content
  - Writes to a file: `~/.audioshuttle/system-prompt.txt` (create dir if needed)
  - Also updates the in-memory SYSTEM_PROMPT that IntentTranslator uses. This requires a function in translator.py to update the prompt — add `update_system_prompt(new_prompt: str)` to translator.py that sets the module-level variable. DO NOT modify translator.py's existing class — just add a module-level function that updates the `SYSTEM_PROMPT` string.
  - Returns `RedirectResponse(url="/input?saved=1", status_code=303)` (post-redirect-get pattern)
  - IMPORTANT: On error, log to error_log and still redirect back

**templates/input.html** — Extends base.html:
- Section: "System Prompt" — large `<textarea>` (rows=20, cols=80, monospace font) pre-filled with current system_prompt value. Wrapping in a `<form method="POST" action="/input/system-prompt">` with a "Save" submit button.
- Section: "AI Client" — display card showing current chat_api_url and chat_model_name. Read-only for now (these come from env vars/config). Show a note: "Configure via AUDIOSHUTTLE_CHAT_API_URL environment variable"
- Section: "Preset AI Clients" — simple list/table of the ai_clients with name and description. No interactive selection yet — just informational.
- If `saved` query param is "1", show a green "✓ Saved" confirmation message at top (use Jinja2 `request.query_params.get('saved')`)
- Dark theme consistent with base.html

**IMPORTANT:** Add `update_system_prompt(new_prompt: str) -> None` function to `src/audioshuttle/translator.py`. This function:
1. Sets the module-level `SYSTEM_PROMPT` variable
2. That's it — IntentTranslator instances read SYSTEM_PROMPT at translate() time, so they'll pick up the change automatically
  </action>
  <verify>cd /home/korphaus/audioshuttle && python -c "
from audioshuttle.web import create_web_app
from audioshuttle.config import Settings
from fastapi.testclient import TestClient

app = create_web_app(Settings())
client = TestClient(app)

# GET /input
resp = client.get('/input')
assert resp.status_code == 200
html = resp.text
assert 'textarea' in html
assert 'system_prompt' in html.lower() or 'System Prompt' in html

# POST /input/system-prompt
resp = client.post('/input/system-prompt', data={'system_prompt': 'Test prompt for E2B'})
assert resp.status_code == 303
assert '/input' in resp.headers['location']

# Verify saved
resp = client.get('/input?saved=1')
assert 'Saved' in resp.text or 'saved' in resp.text.lower()

# Verify translator has update function
from audioshuttle.translator import update_system_prompt
update_system_prompt('New test prompt')
print('Input tab OK')
"</verify>
  <done>Input tab at GET /input shows system prompt textarea. POST /input/system-prompt saves to file and updates in-memory prompt. Save confirmation shown. AI client info displayed read-only. translator.py has update_system_prompt() function.</done>
</task>

<task type="auto">
  <name>Task 2: Create Output tab — DAW preset + rescan + mappings</name>
  <files>src/audioshuttle/web_routes/output_tab.py, src/audioshuttle/templates/output.html</files>
  <action>
Create the Output (DAW Connect) tab:

**web_routes/output_tab.py:**
- `router = APIRouter()`

Routes:
- `GET /output` — renders output.html with:
  - `daw_type: str` — current DAW type from settings ("reaper" or "ardour")
  - `daw_presets: list[dict]` — `[{"id": "reaper", "name": "Reaper", "osc_port": 8000, "feedback_port": 9000}, {"id": "ardour", "name": "Ardour", "osc_port": 3819, "feedback_port": 3819}]`
  - `detection: dict` — result of `detect_daw()` (from daw_detect module)
  - `osc_mappings: list[dict]` — list of OSC address patterns from the bridge. For Reaper, these come from `osc_bridge.py`'s `_ADDRESS_PATTERNS` keys. Format: `[{"pattern": "/track/{n}/volume", "description": "Set track volume"}, ...]`
  - `reaper_host: str`, `reaper_port: int` — from settings
  - `rescanned: bool` — False by default

- `POST /output/daw-preset` — changes DAW preset:
  - Accepts `daw_type: str = Form(...)` — "reaper" or "ardour"
  - Updates settings.daw_type (in memory only for now — env vars override on restart)
  - Logs to error_log: "DAW preset changed to {daw_type}"
  - Returns `RedirectResponse(url="/output?rescanned=1", status_code=303)`

- `POST /output/rescan` — triggers DAW rescan:
  - Calls `detect_daw()` 
  - If a DAW was detected and differs from current setting, log a note
  - Returns `RedirectResponse(url="/output?rescanned=1", status_code=303)`

**templates/output.html** — Extends base.html:
- Section: "DAW Detection" — shows detected DAW result (process name, running status). "Rescan" button (form POST to /output/rescan)
- Section: "DAW Preset" — dropdown `<select>` with reaper and ardour options. Current selection pre-selected. "Apply" button (form POST to /output/daw-preset)
- Section: "Connection" — shows reaper_host and reaper_port from settings. Read-only display.
- Section: "OSC Address Mappings" — a `<table>` with columns: Pattern, Description. Rows from osc_mappings. Simple HTML table, no interactivity. If no bridge available, show "No OSC mappings loaded — bridge not connected"
- If `rescanned` query param is "1", show green "✓ Rescanned" confirmation
- Dark theme consistent with base.html

**IMPORTANT:** For osc_mappings, if `app.state.bridge` is None (no bridge connected), pass an empty list. The template handles the empty case.
  </action>
  <verify>cd /home/korphaus/audioshuttle && python -c "
from audioshuttle.web import create_web_app
from audioshuttle.config import Settings
from fastapi.testclient import TestClient

app = create_web_app(Settings())
client = TestClient(app)

# GET /output
resp = client.get('/output')
assert resp.status_code == 200
html = resp.text
assert 'Reaper' in html
assert 'Ardour' in html
assert 'Rescan' in html or 'rescan' in html.lower()

# POST /output/rescan
resp = client.post('/output/rescan')
assert resp.status_code == 303

# POST /output/daw-preset
resp = client.post('/output/daw-preset', data={'daw_type': 'ardour'})
assert resp.status_code == 303

print('Output tab OK')
"</verify>
  <done>Output tab at GET /output shows DAW detection result, preset dropdown, rescan button, connection info, and OSC mappings table. POST routes handle preset change and rescan with redirect-back pattern.</done>
</task>

</tasks>

<verification>
1. GET /input returns 200 with system prompt textarea
2. POST /input/system-prompt saves and redirects back
3. GET /output returns 200 with DAW preset dropdown and mappings
4. POST /output/rescan triggers detect_daw and redirects
5. POST /output/daw-preset changes preset and redirects
6. All routes use post-redirect-get pattern (303)
7. Nav tabs highlight active tab correctly
8. translator.py has update_system_prompt() function
</verification>

<success_criteria>
- Input tab displays and saves the E2B system prompt (critical demo feature)
- Output tab shows DAW preset selection and OSC mappings
- All form submissions use post-redirect-get pattern
- System prompt save persists to file and updates in-memory translator
</success_criteria>

<output>
After completion, create `.planning/phases/04-web-ui-integration/04-02-SUMMARY.md`
</output>

---
phase: 05-voice-demo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/audioshuttle/stt.py
  - src/audioshuttle/server.py
  - tests/test_stt.py
autonomous: true

must_haves:
  truths:
    - "AudioShuttle can transcribe a WAV file to text using faster-whisper"
    - "STT gracefully degrades when faster-whisper is not installed"
    - "A new MCP tool transcribe_audio exposes STT to LLM clients"
  artifacts:
    - path: "src/audioshuttle/stt.py"
      provides: "STTEngine class with transcribe() method"
      exports: ["STTEngine"]
    - path: "tests/test_stt.py"
      provides: "Unit tests for STT engine with mocked model"
  key_links:
    - from: "src/audioshuttle/stt.py"
      to: "faster_whisper.WhisperModel"
      via: "import with graceful fallback"
      pattern: "faster_whisper"
---

<objective>
Add optional voice transcription capability via faster-whisper.

Purpose: Enable voice commands as input to AudioShuttle. STT is optional — the system works fully without it via text. This makes the demo more impressive and supports the accessibility angle.
Output: STTEngine class, transcribe_audio MCP tool, graceful degradation when no mic/model
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/audioshuttle/server.py
@src/audioshuttle/config.py
@src/audioshuttle/models.py
@pyproject.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create STT engine with faster-whisper integration</name>
  <files>src/audioshuttle/stt.py, tests/test_stt.py</files>
  <action>
Create `src/audioshuttle/stt.py` with:

1. **STTEngine class** with:
   - `__init__(self, model_size: str = "base")` — lazy-loads WhisperModel
   - `transcribe(self, audio_path: str) -> str` — returns transcribed text
   - `_load_model()` — tries to import faster_whisper, catches ImportError
   - Class-level `available: bool` property — True if faster_whisper installed
   - Thread-safe singleton pattern (like ErrorLog)

2. **Graceful degradation:**
   - If faster_whisper not installed: `transcribe()` raises `RuntimeError("faster-whisper not installed. Install with: pip install audioshuttle[stt]")`
   - `available` returns False when not installed
   - Import wrapped in try/except at module level

3. **Audio format support:** WAV (16kHz mono preferred, but handle any ffmpeg-supported format via faster-whisper's built-in conversion)

4. **Model caching:** Use HuggingFace cache (`~/.cache/huggingface/hub/`). First run downloads model (~150MB for "base"), subsequent runs use cache.

5. **Config integration:** Add to config.py:
   - `stt_model_size: str = "base"` (tiny/base/small/medium/large-v3)
   - `stt_device: str = "cpu"` (cpu or cuda, default cpu for stability)
   - `stt_compute_type: str = "int8"` (int8 for CPU, float16 for GPU)

Create `tests/test_stt.py` with:
- Test STTEngine.available is True/False based on import
- Test transcribe() with a mock WhisperModel (create a tiny WAV using struct/wave in test)
- Test graceful degradation when faster_whisper not importable (mock ImportError)
- Test singleton pattern

Do NOT actually install faster-whisper or download models in tests — mock everything. The `[stt]` optional dep is already declared in pyproject.toml.
  </action>
  <verify>
    cd /home/korphaus/audioshuttle && .venv/bin/python -m pytest tests/test_stt.py -v
  </verify>
  <done>
    - STTEngine class with transcribe() method
    - Graceful degradation when faster-whisper not installed
    - All tests passing without faster-whisper actually installed
    - Config has stt_model_size, stt_device, stt_compute_type settings
  </done>
</task>

<task type="auto">
  <name>Task 2: Add transcribe_audio MCP tool to server</name>
  <files>src/audioshuttle/server.py</files>
  <action>
Add a new MCP tool `transcribe_audio` to the existing FastMCP server in `src/audioshuttle/server.py`:

1. **Tool signature:** `transcribe_audio(audio_path: str) -> str`
   - Parameter: audio_path (str) — path to audio file (WAV, MP3, etc.)
   - Returns: transcribed text

2. **Implementation:**
   - Import STTEngine from stt module
   - Check `STTEngine.available` first — if False, return helpful error message explaining how to install
   - Create/get STTEngine instance, call transcribe()
   - Return transcribed text

3. **Tool description:** "Transcribe an audio file to text using Whisper. Requires audioshuttle[stt] optional dependency. Returns transcribed text that can be used as a voice command."

4. **Integration:** The transcribe_audio tool should be usable in a voice command pipeline:
   - User records audio → file saved → LLM calls transcribe_audio → gets text → calls interpret_command → DAW executes

5. **Update tool count in any docstrings/comments** (was 19, now 20 tools)

Do NOT modify the existing interpret_command tool — transcribe_audio is a separate, standalone tool.
  </action>
  <verify>
    cd /home/korphaus/audioshuttle && .venv/bin/python -m pytest --tb=short -q
  </verify>
  <done>
    - transcribe_audio MCP tool registered in FastMCP server
    - Tool handles missing faster-whisper gracefully with install instructions
    - All 137+ existing tests still pass
  </done>
</task>

</tasks>

<verification>
All tests pass: `.venv/bin/python -m pytest --tb=short -q`
STTEngine available in stt module: `.venv/bin/python -c "from audioshuttle.stt import STTEngine; print(STTEngine.available)"`
transcribe_audio tool registered: `.venv/bin/python -c "from audioshuttle.server import mcp; print('transcribe_audio' in [t.name for t in mcp._tool_manager.tools.values()])"`
</verification>

<success_criteria>
- STTEngine class with transcribe() and available property
- transcribe_audio MCP tool registered
- Graceful degradation when faster-whisper not installed
- All existing tests + new STT tests passing
</success_criteria>

<output>
After completion, create `.planning/phases/05-voice-demo/05-01-SUMMARY.md`
</output>

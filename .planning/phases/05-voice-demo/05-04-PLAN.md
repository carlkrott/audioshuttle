---
phase: 05-voice-demo
plan: 04
type: execute
wave: 2
depends_on: ["05-02"]
files_modified:
  - src/audioshuttle/web_routes/log_tab.py
  - src/audioshuttle/templates/log.html
  - src/audioshuttle/web_routes/output_tab.py
  - src/audioshuttle/templates/output.html
  - src/audioshuttle/web_routes/shortcuts.py
  - src/audioshuttle/templates/shortcuts.html
autonomous: true

must_haves:
  truths:
    - "Command log tab shows color-coded history of all MCP tool calls"
    - "Output tab has track preset save/load and state export"
    - "Shortcuts reference page lists all 20 MCP tools with examples"
  artifacts:
    - path: "src/audioshuttle/web_routes/log_tab.py"
      provides: "Log tab route with color-coded command history"
      contains: "router"
    - path: "src/audioshuttle/web_routes/shortcuts.py"
      provides: "Shortcuts reference route listing all MCP tools"
      contains: "router"
  key_links:
    - from: "src/audioshuttle/web_routes/log_tab.py"
      to: "src/audioshuttle/error_log.py"
      via: "command log data source"
      pattern: "get_recent"
---

<objective>
Add command log tab, output tab enhancements (track presets, state export), and keyboard shortcuts reference.

Purpose: Three smaller features that fill out the web UI. The log tab gives visibility into what commands were executed. Track presets let users save/restore mixer states. Shortcuts reference helps new users discover MCP tools.
Output: Log tab, enhanced output tab, shortcuts reference page
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-voice-demo/05-CONTEXT.md
@src/audioshuttle/web.py
@src/audioshuttle/web_routes/output_tab.py
@src/audioshuttle/templates/output.html
@src/audioshuttle/error_log.py
@src/audioshuttle/server.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Command log tab with color-coded history</name>
  <files>src/audioshuttle/web_routes/log_tab.py, src/audioshuttle/templates/log.html</files>
  <action>
Create `src/audioshuttle/web_routes/log_tab.py`:

1. **APIRouter** for the log tab
2. **GET `/log`** — renders log.html with:
   - All entries from `app.state.error_log.get_recent(200)`
   - Each entry has: timestamp, level (INFO/WARNING/ERROR), message
3. **GET `/log/filter`** — accepts `level` query param (all/info/warning/error):
   - Filters entries by level
   - Returns filtered list to template

Create `src/audioshuttle/templates/log.html`:

1. **Filter bar** at top: All | Info | Warning | Error — clickable links that filter
2. **Log entries** — scrollable list (max-height: 500px, overflow-y: auto):
   - Each entry on one line: `[timestamp] LEVEL: message`
   - Color coding:
     - Info/success: green text (`#0f9b58`)
     - Warning: yellow text (`#fbbc04`)
     - Error: red text (`#ea4335`)
   - Monospace font (same as error log in home.html)
3. **Auto-scroll** — vanilla JS to scroll to bottom on load (most recent entries at bottom)
4. **Match existing dark theme** with inline CSS
  </action>
  <verify>
    cd /home/korphaus/audioshuttle && .venv/bin/python -c "
from audioshuttle.web import create_web_app
app = create_web_app()
print('/log' in [r.path for r in app.routes])
"
  </verify>
  <done>
    - Log tab renders at /log with color-coded entries
    - Filter links work (all/info/warning/error)
    - Auto-scrolls to bottom
    - Route registered via existing ImportError pattern in web.py
  </done>
</task>

<task type="auto">
  <name>Task 2: Track presets + state snapshot + shortcuts reference</name>
  <files>src/audioshuttle/web_routes/output_tab.py, src/audioshuttle/templates/output.html, src/audioshuttle/web_routes/shortcuts.py, src/audioshuttle/templates/shortcuts.html</files>
  <action>
**Enhance Output Tab** (`src/audioshuttle/web_routes/output_tab.py` + `src/audioshuttle/templates/output.html`):

1. **Track Presets section** — below existing DAW preset area:
   - "Save Current Mix" button → POST `/output/preset/save` with name input
   - Saves all track volumes/mutes/solos/pans + master volume to `~/.audioshuttle/presets/{name}.json`
   - "Load Preset" dropdown listing saved presets → POST `/output/preset/load`
   - Load restores all settings via OSC commands
   - Create `~/.audioshuttle/presets/` directory if not exists

2. **DAW State Snapshot** section:
   - "Export State" button → GET `/output/snapshot` returns JSON download
   - JSON includes: all track states, master, transport, tempo (if available)
   - Use `bridge.get_full_state()` or assemble from known state

3. **Add routes:**
   - POST `/output/preset/save` — accepts `name` form field, writes JSON
   - POST `/output/preset/load` — accepts `preset_name` form field, reads JSON, sends OSC
   - GET `/output/snapshot` — returns JSON response with `media_type="application/json"`

**Create Shortcuts Reference** (`src/audioshuttle/web_routes/shortcuts.py` + `src/audioshuttle/templates/shortcuts.html`):

1. **GET `/shortcuts`** — renders shortcuts.html with all 20 MCP tools

2. **Tool reference table** — hardcoded in template (or generated from server.py tool definitions):
   - Columns: Tool Name, Category, Description, Example Command, Parameters
   - Grouped by category:
     - Transport: play, stop, record, pause, seek
     - Track: set_volume, set_mute, set_solo, set_pan, set_arm
     - Master: set_master_volume, set_master_pan
     - FX: set_fx_param, toggle_fx_bypass
     - DAW: get_daw_state, refresh_state, execute_action
     - AI: interpret_command, transcribe_audio
     - System: check_health
   - Search/filter input at top (vanilla JS, filters table rows by text match)

3. **Styling:** Match dark theme. Table with alternating row colors. Example commands in monospace.
  </action>
  <verify>
    cd /home/korphaus/audioshuttle && .venv/bin/python -m pytest tests/test_web.py --tb=short -q
  </verify>
  <done>
    - Output tab has Save/Load preset buttons that persist mixer state as JSON
    - Export State returns full DAW state as downloadable JSON
    - Shortcuts page lists all 20 MCP tools with examples and search
    - All routes registered and working
    - All tests pass
  </done>
</task>

</tasks>

<verification>
All tests pass: `.venv/bin/python -m pytest --tb=short -q`
Log tab route: `.venv/bin/python -c "from audioshuttle.web import create_web_app; app = create_web_app(); print('/log' in [r.path for r in app.routes])"`
Shortcuts route: `.venv/bin/python -c "from audioshuttle.web import create_web_app; app = create_web_app(); print('/shortcuts' in [r.path for r in app.routes])"`
</verification>

<success_criteria>
- Command log tab with color-coded entries and filtering
- Track preset save/load on output tab
- DAW state export as JSON
- Shortcuts reference page with all 20 MCP tools
- All existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/05-voice-demo/05-04-SUMMARY.md`
</output>

---
phase: 05-voice-demo
plan: 04
type: execute
wave: 4
depends_on: ["05-01", "05-02", "05-03"]
files_modified:
  - src/audioshuttle/web_routes/home.py
  - src/audioshuttle/templates/home.html
  - src/audioshuttle/launcher.py
  - src/audioshuttle/tray.py
  - tests/test_web.py
autonomous: false

must_haves:
  truths:
    - "Home page shows connection status for all components at a glance"
    - "Launcher handles missing model binary gracefully"
    - "System tray provides quick access to key actions"
    - "End-to-end flow works: launch → web UI → command → Reaper responds"
  artifacts:
    - path: "src/audioshuttle/launcher.py"
      provides: "Robust launcher with error recovery"
    - path: "src/audioshuttle/templates/home.html"
      provides: "Polished dashboard with all statuses"
  key_links:
    - from: "src/audioshuttle/launcher.py"
      to: "src/audioshuttle/model_server.py"
      via: "model server lifecycle management"
      pattern: "ModelServer"
---

<objective>
Polish and harden the end-to-end experience for demo readiness.

Purpose: The demo needs to work smoothly without hiccups. Edge cases, error handling, and visual polish make the difference between a good and great submission.
Output: Hardened launcher, polished web UI, verified E2E flow
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/audioshuttle/launcher.py
@src/audioshuttle/tray.py
@src/audioshuttle/web_routes/home.py
@src/audioshuttle/templates/home.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Harden launcher and error handling</name>
  <files>src/audioshuttle/launcher.py, src/audioshuttle/tray.py</files>
  <action>
Improve `src/audioshuttle/launcher.py`:

1. **Model server robustness:**
   - If llama-server binary not found: log warning, continue without model (text-only mode)
   - If model file not found: log warning, continue without model
   - If model server fails to start within timeout: log error, continue
   - Add `--no-model` CLI flag to skip model server entirely

2. **Startup sequence hardening:**
   - Each component (model, web, tray, bridge) starts independently
   - Failure of one doesn't crash the others
   - Log which components started successfully
   - Web UI shows which components are active

3. **Graceful shutdown:**
   - SIGINT/SIGTERM handler that stops components in reverse order
   - Stop: web server → model server → tray → bridge
   - Clean up temp files, OSC connections

4. **Launcher status reporting:**
   - `get_status()` method returning dict of component states
   - Web UI can poll this for live status updates

Improve `src/audioshuttle/tray.py`:
   - If model not running: show "AudioShuttle (text-only)" in tray tooltip
   - Add "Restart Model Server" menu item (if model server exists)
   - Show model status in tray tooltip: "AudioShuttle — Model: running | Reaper: connected"
</action>
  <verify>
    cd /home/korphaus/audioshuttle && .venv/bin/python -m pytest --tb=short -q
  </verify>
  <done>
    - Launcher starts even when model binary/file missing
    - SIGINT/SIGTERM gracefully shuts down all components
    - Tray shows component status in tooltip
    - All tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Polish web UI home dashboard</name>
  <files>src/audioshuttle/web_routes/home.py, src/audioshuttle/templates/home.html</files>
  <action>
Polish `src/audioshuttle/templates/home.html`:

1. **Component status cards:**
   - Reaper: green "Connected" / red "Not Running" badge
   - AI Model: green "Active (Gemma E2B)" / yellow "Not Started" / gray "Not Installed"
   - MCP Server: green "Ready" / yellow "Starting..."
   - STT: green "Available" / gray "Not Installed (pip install audioshuttle[stt])"
   - Each card shows brief info (model name, port, etc.)

2. **Quick Actions section:**
   - "Test Connection" button that pings Reaper OSC
   - "Refresh Status" button
   - Link to "Record Voice Command" (scrolls to input tab voice section)

3. **Activity feed:**
   - Show last 5 commands executed (from error_log or a new activity log)
   - Timestamp + command text + status (success/error)

4. **Visual polish:**
   - Subtle gradient on status cards
   - Smooth transitions on status changes
   - Better spacing and typography
   - Mobile-responsive (stack cards vertically on narrow screens)

Update `src/audioshuttle/web_routes/home.py`:
   - Fetch launcher status for component states
   - Fetch last N activity entries
   - Pass to template as context

Do NOT add JavaScript frameworks. Vanilla JS only for any dynamic updates.
  </action>
  <verify>
    cd /home/korphaus/audioshuttle && .venv/bin/python -m pytest tests/test_web.py --tb=short -q
  </verify>
  <done>
    - Home page shows component status cards with live data
    - Quick actions section functional
    - Activity feed shows recent commands
    - Visual polish applied
    - All tests pass
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete Phase 5 deliverables:
1. STT engine with faster-whisper (optional)
2. Browser voice recording in web UI
3. README + demo walkthrough + CI
4. Hardened launcher + polished dashboard
  </what-built>
  <how-to-verify>
1. **Start AudioShuttle:** `cd ~/audioshuttle && .venv/bin/python -m audioshuttle.cli`
2. **Open web UI:** Browser should open to http://localhost:8765
3. **Check home dashboard:** Verify status cards show Reaper/model/STT status
4. **Test text command:** In the web UI input tab, or via Gemini CLI, send "play" and verify Reaper transport starts
5. **Test voice (if mic available):** Click Record in input tab, speak a command, verify transcription appears
6. **Check system tray:** Verify tray icon shows with status tooltip
7. **Verify README:** Open README.md and confirm it reads well
8. **Graceful shutdown:** Ctrl+C the launcher, verify clean exit
  </how-to-verify>
  <resume-signal>Type "approved" to confirm Phase 5 complete, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
All tests pass: `.venv/bin/python -m pytest --tb=short -q`
README exists: `test -f /home/korphaus/audioshuttle/README.md`
Demo walkthrough exists: `test -f /home/korphaus/audioshuttle/examples/demo_walkthrough.md`
</verification>

<success_criteria>
- Launcher handles missing components gracefully
- Home dashboard shows all component statuses
- Voice recording works in browser (with mic)
- README is submission-ready
- Human verified E2E flow works
</success_criteria>

<output>
After completion, create `.planning/phases/05-voice-demo/05-04-SUMMARY.md`
</output>

---
phase: 05-voice-demo
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/audioshuttle/web_routes/home.py
  - src/audioshuttle/templates/home.html
  - src/audioshuttle/templates/base.html
  - src/audioshuttle/web.py
  - tests/test_web.py
autonomous: true

must_haves:
  truths:
    - "Home page shows connection pulse, quick transport buttons, model status, and command history"
    - "Nav bar includes new tabs: MIDI, Log, Shortcuts"
    - "New tab route stubs are registered in web.py"
  artifacts:
    - path: "src/audioshuttle/templates/home.html"
      provides: "Enhanced dashboard with 4 new widget sections"
      min_lines: 80
    - path: "src/audioshuttle/templates/base.html"
      provides: "Nav with Home, Input, Output, MIDI, Log, Shortcuts"
      contains: "MIDI"
  key_links:
    - from: "src/audioshuttle/web_routes/home.py"
      to: "src/audioshuttle/error_log.py"
      via: "command history from error log"
      pattern: "get_recent"
---

<objective>
Enhance the home dashboard with live status widgets and quick actions. Register all new tab routes and update navigation.

Purpose: The home tab is the first thing users see. It needs to feel alive — pulsing connection indicator, one-click transport controls, model status with latency, and a scrollable command history. This plan also sets up the route infrastructure for Plans 03, 04 so they don't conflict on web.py.
Output: Polished home dashboard, nav with 6 tabs, route stubs for MIDI/Log/Shortcuts
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-voice-demo/05-CONTEXT.md
@src/audioshuttle/web_routes/home.py
@src/audioshuttle/templates/home.html
@src/audioshuttle/templates/base.html
@src/audioshuttle/web.py
@src/audioshuttle/error_log.py
@src/audioshuttle/osc_bridge.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enhance home dashboard with status widgets + quick actions</name>
  <files>src/audioshuttle/web_routes/home.py, src/audioshuttle/templates/home.html</files>
  <action>
Update `src/audioshuttle/templates/home.html` to add these sections below the existing status row:

1. **Live Connection Pulse** — Replace static DAW badge with animated indicator:
   - Green pulsing dot (CSS `@keyframes pulse`) when `daw_connected` is True
   - Static red dot when disconnected
   - Text: "Reaper Connected" / "Reaper Offline"

2. **Quick Transport Buttons** — Horizontal row of buttons:
   - ▶ Play, ⏹ Stop, ⏺ Record, ⏸ Pause
   - Each is a form that POSTs to `/home/transport` with `action=play|stop|record|pause`
   - Styled as rounded dark buttons with colored icons (green play, red record, etc.)

3. **Model Status Card** — Box showing:
   - Model state badge (running/loading/disabled)
   - If running: last inference time in ms, average tokens/sec
   - GPU VRAM usage bar (already exists, keep it)
   - Store inference stats on `app.state.model_server` or a new `app.state.model_stats` dict

4. **Command History** — Scrollable list of last 20 commands:
   - Each entry: timestamp, command text, tool called, status (success/error)
   - Click to re-execute (link to `/home/replay?cmd=<encoded_command>`)
   - Green text for success, red for errors
   - Get data from `error_log.get_recent(20)` filtered for command-level entries, or create a new `command_history` deque on `app.state`

Update `src/audioshuttle/web_routes/home.py`:

1. Add POST `/home/transport` route — accepts `action` form field, sends OSC command via `app.state.bridge`, redirects back to `/`

2. Add GET `/home/replay` route — accepts `cmd` query param, calls `app.state.translator.translate(cmd)` then executes result, redirects to `/`

3. Add model stats to the status dict:
   - `model_inference_ms` — last inference duration (store on model_server)
   - `model_tokens_sec` — average throughput (store on model_server)

4. Add command history to template context:
   - `history` list from `app.state` command_history deque (maxlen=100)

Styling: All inline CSS in the template (match existing dark theme). No JavaScript frameworks. Use CSS animations for the pulse effect.
  </action>
  <verify>
    cd /home/korphaus/audioshuttle && .venv/bin/python -m pytest tests/test_web.py --tb=short -q
  </verify>
  <done>
    - Home page shows animated connection pulse
    - Quick transport buttons (Play/Stop/Record/Pause) functional
    - Model status card with inference stats
    - Command history with replay links
    - All web tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Register new tab routes + update navigation</name>
  <files>src/audioshuttle/web.py, src/audioshuttle/templates/base.html</files>
  <action>
Update `src/audioshuttle/web.py` to register route stubs for new tabs:

```python
# After existing route registrations, add:
try:
    from audioshuttle.web_routes.midi_tab import router as midi_router
    app.include_router(midi_router)
except ImportError:
    pass

try:
    from audioshuttle.web_routes.log_tab import router as log_router
    app.include_router(log_router)
except ImportError:
    pass

try:
    from audioshuttle.web_routes.shortcuts import router as shortcuts_router
    app.include_router(shortcuts_router)
except ImportError:
    pass
```

Update `src/audioshuttle/templates/base.html` nav section to include all tabs:

```html
<nav>
    <span class="brand">AudioShuttle</span>
    <a href="/" class="{{ 'active' if request.url.path == '/' }}">Home</a>
    <a href="/input" class="{{ 'active' if request.url.path == '/input' }}">Input</a>
    <a href="/output" class="{{ 'active' if request.url.path == '/output' }}">Output</a>
    <a href="/midi" class="{{ 'active' if request.url.path == '/midi' }}">MIDI</a>
    <a href="/log" class="{{ 'active' if request.url.path == '/log' }}">Log</a>
    <a href="/shortcuts" class="{{ 'active' if request.url.path == '/shortcuts' }}">Shortcuts</a>
</nav>
```

Do NOT create the actual route files (midi_tab.py, log_tab.py, shortcuts.py) — Plans 03 and 04 will create them. The try/except ImportError pattern means missing routes are silently skipped.
  </action>
  <verify>
    cd /home/korphaus/audioshuttle && .venv/bin/python -c "
from audioshuttle.web import create_web_app
app = create_web_app()
routes = [r.path for r in app.routes]
print('Routes:', [r for r in routes if r in ['/', '/input', '/output', '/midi', '/log', '/shortcuts']])
"
  </verify>
  <done>
    - Nav has 6 tabs: Home, Input, Output, MIDI, Log, Shortcuts
    - Route stubs registered with ImportError fallback
    - Existing routes still work
    - All tests pass
  </done>
</task>

</tasks>

<verification>
All tests pass: `.venv/bin/python -m pytest --tb=short -q`
Nav has 6 tabs: grep for "MIDI" and "Log" in base.html
Web app starts: `.venv/bin/python -c "from audioshuttle.web import create_web_app; create_web_app()"`
</verification>

<success_criteria>
- Home dashboard has connection pulse, transport buttons, model status, command history
- Navigation includes MIDI, Log, Shortcuts tabs
- Route stubs registered for all new tabs
- All existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/05-voice-demo/05-02-SUMMARY.md`
</output>

---
phase: 05-voice-demo
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/audioshuttle/voice.py
  - src/audioshuttle/web_routes/input_tab.py
  - src/audioshuttle/templates/input.html
  - tests/test_voice.py
autonomous: true

must_haves:
  truths:
    - "User can record audio from browser and get it transcribed"
    - "Voice recording works in the web UI input tab"
    - "Recorded audio is sent to STT and result displayed"
  artifacts:
    - path: "src/audioshuttle/voice.py"
      provides: "Voice command pipeline: record → transcribe → execute"
      exports: ["VoicePipeline"]
    - path: "src/audioshuttle/templates/input.html"
      provides: "Updated input tab with voice recording UI"
  key_links:
    - from: "src/audioshuttle/web_routes/input_tab.py"
      to: "src/audioshuttle/voice.py"
      via: "POST /input/voice endpoint"
      pattern: "VoicePipeline"
---

<objective>
Add browser-based voice recording and transcription to the web UI.

Purpose: The web UI already has the Input tab with system prompt editor. Adding a "Record Voice" button lets users speak commands directly in the browser, making the demo interactive and showcasing the accessibility angle.
Output: Voice recording UI in browser, POST endpoint for audio upload, transcription display
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/audioshuttle/stt.py
@src/audioshuttle/server.py
@src/audioshuttle/web_routes/input_tab.py
@src/audioshuttle/templates/input.html
@src/audioshuttle/templates/base.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create voice pipeline + web endpoint</name>
  <files>src/audioshuttle/voice.py, src/audioshuttle/web_routes/input_tab.py, tests/test_voice.py</files>
  <action>
Create `src/audioshuttle/voice.py` with:

1. **VoicePipeline class:**
   - `__init__(self, stt_engine, translator=None, bridge=None)`
   - `async def process_audio(self, audio_bytes: bytes, filename: str) -> dict`
     - Saves audio to temp file (using tempfile.NamedTemporaryFile)
     - Calls STTEngine.transcribe() on the temp file
     - Returns `{"transcription": str, "success": bool, "error": str|None}`
   - If STT not available: returns `{"transcription": "", "success": False, "error": "STT not available. Install audioshuttle[stt]"}`
   - Cleanup temp file after transcription

2. **Add POST /input/voice route** to `src/audioshuttle/web_routes/input_tab.py`:
   - Accept multipart form upload with audio file (`audio_file` field)
   - Validate file is audio (check content-type or extension: .wav, .mp3, .ogg, .webm)
   - Read bytes, pass to VoicePipeline.process_audio()
   - Return JSON response: `{"transcription": str, "success": bool, "error": str|None}`
   - Handle file size limit (max 10MB)

3. **Add static upload directory** (if not exists): `~/.audioshuttle/uploads/` for any persisted audio (or just use temp files)

4. **Web route registration:** Ensure the new POST route is registered in the FastAPI app. Check how existing routes are registered (likely via `app.include_router()` or direct `@app.post()`).

Create `tests/test_voice.py`:
- Test VoicePipeline.process_audio() with mocked STTEngine
- Test POST /input/voice with a small test WAV file (create in test using wave module)
- Test rejection of non-audio files
- Test rejection of oversized files
- Test graceful handling when STT not available

Use httpx AsyncClient for testing the FastAPI endpoint (follow existing test_web.py patterns).
  </action>
  <verify>
    cd /home/korphaus/audioshuttle && .venv/bin/python -m pytest tests/test_voice.py -v
  </verify>
  <done>
    - VoicePipeline class processes audio bytes → transcription
    - POST /input/voice accepts audio upload, returns transcription JSON
    - Tests pass with mocked STT
  </done>
</task>

<task type="auto">
  <name>Task 2: Add browser voice recording UI to input tab</name>
  <files>src/audioshuttle/templates/input.html</files>
  <action>
Update `src/audioshuttle/templates/input.html` to add voice recording UI:

1. **Add a "🎙 Record Voice Command" section** below the system prompt editor:
   - A record button (red circle icon using Unicode or inline SVG)
   - A stop button (appears when recording)
   - Recording indicator (pulsing red dot + "Recording..." text)
   - Transcription result area (shows after recording stops)

2. **Use MediaRecorder API** (built into modern browsers, no dependencies):
   ```javascript
   // Record audio from getUserMedia
   navigator.mediaDevices.getUserMedia({audio: true})
     .then(stream => {
       const recorder = new MediaRecorder(stream, {mimeType: 'audio/webm'});
       // collect chunks, on stop: send to /input/voice
     });
   ```

3. **Upload flow:**
   - On stop: create FormData with audio blob
   - POST to /input/voice
   - Show spinner during transcription
   - Display transcription result in a styled box
   - Add "Send as Command" button that sends transcription to interpret_command (or shows it in a text field for review)

4. **Styling** (inline CSS, match existing dark theme):
   - Record button: rounded, red (#ef4444), hover glow
   - Recording state: pulsing animation
   - Result box: same style as error log entries

5. **Microphone permission handling:**
   - If permission denied: show helpful message "Microphone access required for voice commands"
   - If no microphone: show "No microphone detected"

6. **Conditional display:**
   - Show the voice section always (feature detection is client-side)
   - If STT not installed server-side: show "Voice commands require faster-whisper. Install with: pip install audioshuttle[stt]"

Do NOT add any JavaScript dependencies — pure vanilla JS, no fetch polyfill needed (modern browsers).
  </action>
  <verify>
    cd /home/korphaus/audioshuttle && .venv/bin/python -m pytest tests/test_web.py tests/test_voice.py --tb=short -q
  </verify>
  <done>
    - Input tab has "Record Voice Command" section with record/stop buttons
    - MediaRecorder API captures audio in browser
    - Audio uploaded to POST /input/voice endpoint
    - Transcription result displayed in UI
    - Existing web tests still pass
  </done>
</task>

</tasks>

<verification>
All tests pass: `.venv/bin/python -m pytest --tb=short -q`
VoicePipeline importable: `.venv/bin/python -c "from audioshuttle.voice import VoicePipeline; print('OK')"`
POST /input/voice route exists: check in test_voice.py
</verification>

<success_criteria>
- VoicePipeline class processes audio → transcription
- Browser can record audio and upload to web UI
- Transcription displayed in input tab
- All existing tests still pass
</success_criteria>

<output>
After completion, create `.planning/phases/05-voice-demo/05-02-SUMMARY.md`
</output>

---
phase: 08-hackathon-submission
plan: 03
type: execute
wave: 2
depends_on: ["08-02"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "Docker images build without errors"
    - "docker compose up starts both services without crashing"
    - "E4B model responds to health checks inside the container"
    - "Audioshuttle MCP server starts and connects to the model"
    - "Container logs show clean startup with no file-not-found or permission errors"
  artifacts:
    - path: "Dockerfile"
      provides: "Working audioshuttle image"
    - path: "Dockerfile.e4b"
      provides: "Working E4B model image"
    - path: "docker-compose.yml"
      provides: "Orchestration that actually works"
  key_links:
    - from: "docker-compose.yml"
      to: "e4b healthcheck"
      pattern: "condition: service_healthy"
---

<objective>
Test the Dockerized setup end-to-end.

Build both images, start the stack, verify both services are healthy and the audioshuttle MCP server can communicate with the E4B model server inside the container network.

Purpose: Catch container-specific issues (permissions, networking, volume mounts, missing deps) before shipping.
Output: Verified working Dockerized project.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@Dockerfile
@Dockerfile.e4b
@docker-compose.yml
@.env.example
</context>

<tasks>

<task type="auto">
<name>Build both Docker images</name>
<files>N/A</files>
<action>
Build both Docker images and verify they build successfully:

1. Build audioshuttle image: `docker build -t audioshuttle:latest .`
   - Confirm the exit code is 0
   - Look for any WARNING or ERROR messages in the output
   - Verify the image exists: `docker images audioshuttle`

2. Build E4B model image: `docker build -t audioshuttle-e4b:latest -f Dockerfile.e4b .`
   - Note: The first E4B build will be slow because it compiles llama.cpp from source (~10-30 min)
   - Confirm build succeeds
   - Verify the image exists: `docker images audioshuttle-e4b`
   - If the build takes too long or fails due to dependencies, note the errors and suggest fixes (e.g., use prebuilt llama.cpp binary instead of compiling from source)

3. After builds, confirm both images exist and their sizes:
   - `docker images audioshuttle audioshuttle-e4b --format "table {{.Repository}}\t{{.Tag}}\t{{.Size}}"`
</action>
<verify>docker images audioshuttle audioshuttle-e4b --format "table {{.Repository}}\t{{.Tag}}\t{{.Size}}"</verify>
<done>Both images exist. audioshuttle image is <500MB. e4b image builds successfully.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
<name>Verify audioshuttle container works</name>
<files>N/A</files>
<what-built>Dockerized audioshuttle MCP server image</what-built>
<how-to-verify>
1. Start the audioshuttle container alone (without E4B model, since we may not have model files in the Docker context):
   ```
   docker run --rm -it audioshuttle:latest --help
   ```
   Should show the CLI help text.

2. Test stdio mode starts without crashing:
   ```
   echo '{"jsonrpc":"2.0","id":1,"method":"ping"}' | timeout 5 docker run --rm -i audioshuttle:latest --transport=stdio --no-model 2>/dev/null || echo "Timeout expected - MCP server needs proper init"
   ```

3. Check the image has the expected files:
   ```
   docker run --rm audioshuttle:latest python3 -c "import audioshuttle; print(audioshuttle.__version__)"
   ```
</how-to-verify>
<resume-signal>Type "approved" to continue, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
1. Both Docker images build without errors
2. audioshuttle image runs and responds to CLI
3. E4B image has llama-server binary available
4. Any build issues are documented with fixes
</verification>

<success_criteria>
Docker images build successfully. Audioshuttle commands work in container. Ready for final delivery.
</success_criteria>

<output>
After completion, create `.planning/phases/08-hackathon-submission/08-03-SUMMARY.md`
</output>

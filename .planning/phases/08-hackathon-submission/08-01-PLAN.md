---
phase: 08-hackathon-submission
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/audioshuttle/config.py
  - src/audioshuttle/osc_bridge.py
  - src/audioshuttle/gpu_monitor.py
  - .gitignore
  - .planning/STATE.md
  - .planning/ROADMAP.md
autonomous: true

must_haves:
  truths:
    - "No personal paths, usernames, or machine-specific info in source code"
    - ".gitignore excludes all generated/private files from version control"
    - "Project compiles cleanly after all edits"
    - "PATH/username references replaced with generic placeholders or env vars"
  artifacts:
    - path: "src/audioshuttle/config.py"
      provides: "Clean config with no absolute user paths"
      min_lines: 25
    - path: ".gitignore"
      provides: "Comprehensive ignore rules for venv, models, env files, build artifacts"
      min_lines: 15
  key_links:
    - from: "config.py"
      to: "environment variable fallbacks"
      pattern: "os\\.environ\\.get"
---

<objective>
Security scrub and codebase prep for open-source publishing.

Remove all personal information, hardcoded absolute paths, machine-specific comments, and username references from the source code. Update .gitignore to prevent future leaks.

Purpose: Before we can publish to GitHub or package in Docker, we must ensure no personal data leaks. The security audit found 4 categories of issues to fix.
Output: Clean codebase ready for open-source.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/audioshuttle/config.py
@src/audioshuttle/osc_bridge.py
@src/audioshuttle/gpu_monitor.py
@.gitignore
@.planning/STATE.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
<name>Scrub config.py — remove absolute user paths and machine-specific details</name>
<files>src/audioshuttle/config.py</files>
<action>
In `src/audioshuttle/config.py`, replace hardcoded `/home/korphaus/` absolute paths with sensible empty-string defaults. The existing `AUDIOSHUTTLE_*` env var overrides will continue to work, and the model_server.py already checks these values and falls back gracefully.

Changes to make (lines 22-25):
1. `model_binary: str = "/home/korphaus/llama.cpp/build-rocm/bin/llama-server"` → `model_binary: str = ""` (empty means "look up via PATH or auto-detect")
2. `model_path: str = "/home/korphaus/gemma-4-E4B-it-UD-Q4_K_XL.gguf"` → `model_path: str = ""` (user must set via env var or CLI)
3. `model_mmproj: str = "/home/korphaus/models/llm/gemma-4-e4b-mmproj-BF16.gguf"` → `model_mmproj: str = ""`
4. `model_gpu_device: int = 0  # ROCm device index (0 = RX 6950 XT)` → `model_gpu_device: int = 0  # GPU device index for ROCm/CUDA`

Also add a comment at the top of the settings class:
```
# Paths default to empty strings — configure via AUDIOSHUTTLE_MODEL_PATH,
# AUDIOSHUTTLE_MODEL_BINARY, AUDIOSHUTTLE_MODEL_MMPROJ env vars or CLI args.
# Model file must be provided before the model server can start.
```

Do NOT remove the `model_api_url` field — it's correct as `http://localhost:8093/v1/chat/completions` (generic fallback, not personal info).
</action>
<verify>python -m py_compile src/audioshuttle/config.py</verify>
<done>No /home/korphaus paths remain in config.py. All paths default to "". gpu_device comment is generic.</done>
</task>

<task type="auto">
<name>Scrub osc_bridge.py and gpu_monitor.py — generic username references</name>
<files>src/audioshuttle/osc_bridge.py, src/audioshuttle/gpu_monitor.py</files>
<action>
In `src/audioshuttle/osc_bridge.py`, replace username references with generic text:

1. Line ~653: `"The watcher runs inside Reaper (korphaus user)..."` → `"The watcher runs inside Reaper..."` (remove parenthetical)
2. Line ~670: `"The __startup.lua watcher runs as korphaus..."` → `"The __startup.lua watcher runs as the Reaper user..."`  
3. Line ~3005: `"running as the target user (e.g. korphaus)."` → `"running as the target Reaper user."`
4. Line ~3029: `"the trigger file is owned by a different user (Reaper/korphaus)"` → `"the trigger file is owned by a different user (Reaper process)"`

In `src/audioshuttle/gpu_monitor.py`:
5. Line ~10: `"card_index: DRM card index (0=iGPU, 1=discrete GPU on this system)."` → `"card_index: DRM card index (typically 0=iGPU, 1=dGPU)."`

Search for any other instances of "korphaus", "7995x", or "cachyos" in the source files to ensure nothing was missed.
</action>
<verify>grep -n "korphaus\|7995x\|cachyos" src/audioshuttle/*.py should return 0 results (config paths already fixed); python -m py_compile src/audioshuttle/osc_bridge.py && python -m py_compile src/audioshuttle/gpu_monitor.py</verify>
<done>No korphaus, 7995x, or cachyos references remain in any source file under src/audioshuttle/. User-specific comments replaced with generic text.</done>
</task>

<task type="auto">
<name>Update .gitignore with comprehensive rules</name>
<files>.gitignore</files>
<action>
Replace the minimal 2-line .gitignore with a comprehensive one for a Python MCP project:

```
# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/
dist/
build/
*.egg

# Virtual environments
.venv/
.venv*/
venv/
env/

# Environment
.env
.env.*
!.env.example

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Testing
.pytest_cache/
.coverage
htmlcov/
*.report
*.log

# Models (large GGUF files that must be downloaded separately)
*.gguf

# Reaper
Media/
*.RPP
!example.RPP

# OS
.DS_Store
Thumbs.db

# Planning (internal project docs, personal references)
.planning/

# Config overrides
opencode.json
```

Note: `.planning/` is added because it contains personal/hardware-specific info throughout. It should be git-removed (not just ignored) if currently tracked.

Also check if `opencode.json` exists in the project root — if so, add to gitignore (it may contain local MCP tool paths).
</action>
<verify>cat .gitignore</verify>
<done>.gitignore contains all categories listed above. At least 25 lines with clear section comments.</done>
</task>

<task type="auto">
<name>Update STATE.md and ROADMAP.md for current project state</name>
<files>.planning/STATE.md, .planning/ROADMAP.md</files>
<action>
Update STATE.md to reflect Phase 7 completion (modifier system working, E2E verified) and Phase 8 (hackathon submission) being current:

STATE.md changes:
1. Update "Current Position" to Phase 8
2. Update "Last activity" to 2026-05-16
3. Mark Phase 7 as complete in "Phase Progress" table
4. Add Phase 8 entry
5. Update the "Phase 7 Planning" section to reflect completion
6. Update "Verified Working" table: mark create_genre_project and MIDI generator as ✅ complete

ROADMAP.md changes:
1. Mark Phase 7 checkboxes as done [x] 
2. Add Phase 8 entry after Phase 7 with goal: "Package and submit AudioShuttle for Kaggle Gemma 4 Good Hackathon"
3. Phase 8 plans should list the 6 plans from this submission phase
</action>
<verify>cat .planning/STATE.md | head -20 && cat .planning/ROADMAP.md | tail -20</verify>
<done>STATE.md Phase 7 marked complete, Phase 8 current. ROADMAP.md Phase 7 checked off, Phase 8 added with 6 plans.</done>
</task>

</tasks>

<verification>
1. `grep -rn "korphaus\|7995x\|cachyos" src/audioshuttle/*.py` returns 0 results
2. `.gitignore` has at least 25 lines covering all categories
3. `python -m py_compile src/audioshuttle/config.py src/audioshuttle/osc_bridge.py src/audioshuttle/gpu_monitor.py` succeeds
4. STATE.md shows Phase 8 current, ROADMAP.md has Phase 8 entry
</verification>

<success_criteria>
Codebase is clean of personal info. .gitignore prevents future leaks. Project state docs are accurate.
</success_criteria>

<output>
After completion, create `.planning/phases/08-hackathon-submission/08-01-SUMMARY.md`
</output>

---
phase: 06-model-driven-projects
plan: 03
type: execute
wave: 3
depends_on: ["06-02"]
files_modified:
  - src/audioshuttle/translator.py
  - src/audioshuttle/server.py
  - tests/test_e2e_genre.py
autonomous: true

must_haves:
  truths:
    - "E2B model can detect genre from natural language: 'create a rock project' → genre=rock"
    - "E2B model can detect genre from natural language: 'make me a jazz track' → genre=jazz"
    - "When no genre specified, E2B defaults to 'rock': 'create a project' → genre=rock"
    - "E2B can handle genre + customizations: 'create a pop song with piano and strings' → genre=pop, instruments override"
    - "E2B can set custom tempo: 'create a metal project at 180 bpm' → genre=metal, tempo=180"
    - "daw_command is the primary MCP entry point and dispatches create_genre_project"
  artifacts:
    - path: "src/audioshuttle/translator.py"
      provides: "Updated SYSTEM_PROMPT with genre knowledge and create_genre_project tool"
      contains: "create_genre_project"
    - path: "src/audioshuttle/server.py"
      provides: "MCP tool dispatch for create_genre_project"
      contains: "create_genre_project"
    - path: "tests/test_e2e_genre.py"
      provides: "End-to-end integration tests"
      min_lines: 60
  key_links:
    - from: "daw_command('create a rock project')"
      to: "create_genre_project()"
      via: "E2B model translation → tool dispatch"
      pattern: "create_genre_project"
    - from: "SYSTEM_PROMPT genre examples"
      to: "translate_multi() parsing"
      via: "E2B sees genre examples in prompt and maps user input"
      pattern: "genre|rock|jazz|electronic|create_genre_project"
    - from: "server.py _execute_tool()"
      to: "osc_bridge.create_genre_project()"
      via: "lambda dispatch"
      pattern: "create_genre_project.*bridge"
---

<objective>
Wire the model-driven project generation into the MCP server so E2B can detect genres from natural language, choose appropriate parameters, and call `create_genre_project` through the existing `daw_command` flow. Update the system prompt with genre knowledge and register the new tool.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/audioshuttle/translator.py                  # SYSTEM_PROMPT, TOOL_SCHEMAS, translate_multi
@src/audioshuttle/server.py                       # daw_command, _execute_tool dispatch dict
@src/audioshuttle/osc_bridge.py                   # create_genre_project() built by Plan 02
@src/audioshuttle/genre_profiles.py               # Genre data built by Plan 01
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update SYSTEM_PROMPT with genre knowledge and create_genre_project tool</name>
  <files>src/audioshuttle/translator.py</files>
  <action>
Update three things in `translator.py`:

**1. Add `create_genre_project` to TOOL_SCHEMAS (around line 52, after generate_project):**
```python
"create_genre_project": {
    "genre": str,
    "tempo": Optional[int],
    "key": Optional[str],
    "scale": Optional[str],
    "custom_instruments": Optional[list[str]],
    "custom_sections": Optional[list[dict[str, str | int]]],
},
```

**2. Update SYSTEM_PROMPT (around line 118, after generate_project description):**
Add this block after the existing `generate_project` description:

```
119: - create_genre_project: {"genre": str, "tempo": int|null, "key": str, "scale": str, "custom_instruments": list[str]|null, "custom_sections": list|null}
   Creates a complete genre-aware project with auto-routing, buses, FX chains per track, and per-section MIDI.
   PREFER this over generate_project when user mentions a genre or wants a complete project setup.
   
   Genre detection — map user request to one of these genres:
   - "rock", "metal", "punk", "grunge", "alternative" → genre="rock"
   - "pop", "dance", "edm", "electro", "house", "techno", "trance", "dubstep" → genre="electronic"  
   - "hip hop", "rap", "trap", "hiphop", "rnb" → genre="hiphop"
   - "jazz", "swing", "bebop", "fusion" → genre="jazz"
   - "classical", "orchestral", "symphony", "film score" → genre="orchestral"
   - "ambient", "atmospheric", "drone", "soundscape" → genre="ambient"
   - "funk", "soul" → genre="funk"
   - "blues", "delta blues" → genre="blues"
   - "reggae", "dub", "ska" → genre="reggae"
   - If NO genre detected, use genre="rock" as default
   
   Tempo detection — extract BPM if user specifies:
   - "at 140 bpm", "140 bpm", "tempo 140" → tempo=140
   - If not specified, use genre's default tempo
   
   Instrument overrides — respect user requests:
   - "with piano and strings" → custom_instruments=["keys", "strings"]
   - "just drums and bass" → custom_instruments=["drums", "bass"]
   - If not specified, use genre's default instruments
   
   Section overrides — respect user requests:
   - "intro verse chorus" → custom_sections=[{"name":"Intro","bars":4},{"name":"Verse","bars":16},{"name":"Chorus","bars":8}]
   - If not specified, use genre's default sections
   
   SINGLE command — do NOT split into multiple calls. This tool handles everything: tempo, markers, tracks, plugins, MIDI, buses, FX chains, and routing.
```

Also update the "Rules" section (around line 147) to add:
```
- "create project"/"generate project"/"new project"/"set up song" AND genre mentioned → create_genre_project (prefer over generate_project)
- "create project"/"generate project" WITHOUT genre → create_genre_project with genre="rock" (default)
```

Update the example section (after line 161):
```
162: "create a rock project" → {"tool":"create_genre_project","args":{"genre":"rock"}}
   "make me a jazz track at 140 bpm" → {"tool":"create_genre_project","args":{"genre":"jazz","tempo":140}}
   "create an EDM banger" → {"tool":"create_genre_project","args":{"genre":"electronic"}}
   "new project with drums and bass" → {"tool":"create_genre_project","args":{"genre":"rock","custom_instruments":["drums","bass"]}}
   "create a pop song with piano and strings at 100 bpm" → {"tool":"create_genre_project","args":{"genre":"rock","tempo":100,"custom_instruments":["keys","strings"]}}
```

**IMPORTANT:** The updated SYSTEM_PROMPT must NOT break existing tool recognition. Only ADD the new tool and genre guidance. Keep all existing tool descriptions and examples. The E2B model must still be able to call `generate_project`, `set_tempo`, `create_send`, etc. independently when user doesn't want a full genre project.
  </action>
  <verify>python -c "
from audioshuttle.translator import SYSTEM_PROMPT, TOOL_SCHEMAS
assert 'create_genre_project' in TOOL_SCHEMAS
assert 'create_genre_project' in SYSTEM_PROMPT
assert 'genre' in SYSTEM_PROMPT.lower()
assert 'rock' in SYSTEM_PROMPT and 'jazz' in SYSTEM_PROMPT
assert 'generate_project' in SYSTEM_PROMPT  # backward compat
print('SYSTEM_PROMPT updated:', len(SYSTEM_PROMPT), 'chars')
print('TOOL_SCHEMAS tools:', list(TOOL_SCHEMAS.keys()))
"</verify>
  <done>SYSTEM_PROMPT includes genre detection rules for 10+ genres. create_genre_project registered in TOOL_SCHEMAS. Existing tools still present. Backward compatible.</done>
</task>

<task type="auto">
  <name>Task 2: Wire create_genre_project into MCP server dispatch</name>
  <files>src/audioshuttle/server.py</files>
  <action>
Register `create_genre_project` in the `_execute_tool()` dispatch dictionary in `server.py` (around line 95-206).

Find the `_execute_tool()` function or the dict where tools are dispatched. Add:
```python
"create_genre_project": lambda args: bridge.create_genre_project(
    genre=args.get("genre", "rock"),
    tempo=args.get("tempo"),
    key=args.get("key", "C"),
    scale=args.get("scale", "major"),
    custom_instruments=args.get("custom_instruments"),
    custom_sections=args.get("custom_sections"),
),
```

The dispatch should handle optional parameters — all args except `genre` are optional. Use `.get()` with defaults for all optional params.

Verify the dispatch works by checking that `daw_command` flow reaches it:
1. User sends "create a rock project" via MCP → `daw_command("create a rock project")`
2. `daw_command` calls `translator.translate_multi(command, state)`
3. E2B returns `{"tool":"create_genre_project","args":{"genre":"rock"}}`
4. `_execute_tool("create_genre_project", {"genre":"rock"})` dispatches to bridge method

No changes needed to `daw_command()` itself — it already handles dispatching any tool in the schema.
  </action>
  <verify>python -c "
from audioshuttle.server import mcp
# Check the lambda dispatch dict exists with create_genre_project
# Run a grep to verify
import ast, inspect
print('Server module loaded successfully')
print(dir(mcp))
"</verify>
  <done>create_genre_project dispatch lambda registered. daw_command can route to it. Optional params handled with defaults.</done>
</task>

<task type="auto">
  <name>Task 3: Write end-to-end genre integration tests</name>
  <files>tests/test_e2e_genre.py</files>
  <action>
Create `tests/test_e2e_genre.py` with integration tests:

1. `test_system_prompt_contains_genre_guidance` — verify SYSTEM_PROMPT has genre detection rules and examples
2. `test_tool_schemas_include_create_genre_project` — TOOL_SCHEMAS has the new tool with correct param types
3. `test_translate_genre_detection_rock` — mock E2B, send "create a rock project", verify translation returns create_genre_project with genre="rock"
4. `test_translate_genre_detection_jazz` — mock E2B, send "make me a jazz track", verify genre="jazz"
5. `test_translate_genre_with_tempo` — mock E2B, send "create a metal project at 180 bpm", verify genre="metal", tempo=180
6. `test_translate_genre_with_instruments` — mock E2B, send "create a pop song with piano and strings", verify genre="rock" (pop→rock mapping), custom_instruments=["keys","strings"]
7. `test_translate_no_genre_defaults_to_rock` — mock E2B, send "create a project", verify genre="rock"
8. `test_translate_backward_compat` — existing commands like "play", "set tempo 140" still work
9. `test_dispatch_create_genre_project` — mock bridge, call _execute_tool with create_genre_project args, verify bridge.create_genre_project called with correct params
10. `test_default_parameters` — dispatch with minimal args (just genre="rock"), verify all optional params use correct defaults

Use mocking:
```python
from unittest.mock import patch, MagicMock
from audioshuttle.translator import IntentTranslator, SYSTEM_PROMPT, TOOL_SCHEMAS
from audioshuttle.server import _execute_tool  # if accessible
```

For translation tests, mock `model_server.chat()` to return specific JSON responses. For dispatch tests, mock the bridge.

Example:
```python
@patch.object(IntentTranslator, '_translate_with_model_multi')
def test_translate_genre_rock(mock_translate):
    mock_translate.return_value = [{"tool": "create_genre_project", "args": {"genre": "rock"}}]
    translator = IntentTranslator(...)
    result = translator.translate_multi("create a rock project", state)
    assert result[0]["tool"] == "create_genre_project"
    assert result[0]["args"]["genre"] == "rock"
```
  </action>
  <verify>cd /home/korphaus/audioshuttle && source .venv/bin/activate && python -m pytest tests/test_e2e_genre.py -v</verify>
  <done>All 10 integration tests pass. Genre detection works. Dispatch routes correctly. Backward compatibility maintained.</done>
</task>

</tasks>

<verification>
E2B model can detect genre from natural language, choose appropriate parameters, and create complete genre-aware projects via MCP.
</verification>

<success_criteria>
- [x] SYSTEM_PROMPT updated with genre detection rules for 10+ genres
- [x] create_genre_project registered in TOOL_SCHEMAS with correct param types
- [x] MCP dispatch routes create_genre_project to bridge method
- [x] Genre→parameter mapping examples in prompt cover common cases
- [x] Default genre (rock) used when no genre detected
- [x] Tempo override and instrument override supported
- [x] Existing tools and commands still work (backward compatible)
- [x] 10 integration tests passing
</success_criteria>

<output>
After completion, create `.planning/phases/06-model-driven-projects/06-03-SUMMARY.md`
</output>
